import styled from 'styled-components';

import { padding, margin, fonts, colors, mixins } from './settings';
import { NormalContent } from './layout';

const StoryContent = NormalContent.extend``;

export { StoryContent };
